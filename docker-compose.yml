services:
  manga-formatter:
    build: .
    ports:
      - "5888:5000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - manga_tmp:/tmp
      - ./mangas:/mangas:ro
    restart: unless-stopped

volumes:
  manga_tmp:
